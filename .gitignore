# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Vite cache (default is node_modules/.vite, but ignore root .vite too just in case)
.vite/

# Local env files (keep .env / .env.example if you use them)
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
lerna-debug.log*

# Coverage / reports
coverage/
*.lcov

# OS / editor junk
.DS_Store
Thumbs.db
.idea/
*.swp
._*

# ESLint cache (if enabled)
.eslintcache

# VS Code (ignore most, but allow curated workspace files if you want)
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Generated build info (redundant safety)
dist/version.txt
# or if you ever generate it at the site root:
# /version.txt

# Your convention: root-only scratch
/_*


